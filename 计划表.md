# LitWatch Python-native 版本计划表

本计划是 LitWatch 的长期版本路线图，供人工随时审核与修改。当前 v2.0 处于 Release Candidate 人工验收阶段；除已标注状态外，不表示其他 v2.x / v3.0 功能已开始、完成或发布。

# LitWatch Architecture Principle

LitWatch adopts a Python-native core architecture.

Python is the single source of truth for all business-critical logic and research intelligence logic.

Browser HTML/CSS/JavaScript may remain for presentation, interaction and visualization.

External systems such as Zotero, scholarly APIs, LLM providers and research tools must be integrated through explicit adapters/interfaces.

After Stack v2.0 Stable, Dify must **not** remain a required runtime dependency.

No business-critical capability may exist only in:

- frontend JavaScript;
- Dify DSL;
- an external integration; or
- an undocumented script.

Existing stable historical Dify DSL files remain immutable for release-history compatibility.

## 1. Python-native Core 的正式边界

“Python-native Core”或“Python-first Architecture”表示 Python 是所有业务关键逻辑和科研智能逻辑的唯一权威实现，包括：

- literature retrieval orchestration；
- provider orchestration；
- deduplication、ranking 与 canonical identity；
- persistence、subscription 与 scheduler；
- Research Radar；
- LLM orchestration 与 pipeline execution；
- knowledge retrieval 与 PDF processing；
- structured paper analysis 与 research synthesis；
- Zotero workflow state。

HTML、CSS 与浏览器 JavaScript 可以继续用于 presentation、interaction 与 visualization，但前端不得成为任何业务规则的唯一实现位置。数据库、学术 API、模型服务和 Zotero 都通过明确接口接入，不能反向成为 LitWatch 核心状态的唯一权威来源。

## 2. 总体版本路线

| Stack 版本 | 正式名称 | 核心交付 | Dify 状态 | 状态 |
|---|---|---|---|---|
| v1.7 | Research Radar Stable | 历史回溯、趋势、证据与扫描历史 | 兼容保留，不在本版移除 | Stable（dify-v1.7） |
| v2.0 | Python-native Runtime Migration | 完成运行时迁移和 Dify-free 验收 | Stable 后不再是必需运行依赖 | Release Candidate（待人工验收） |
| v2.1 | Hybrid Knowledge Base | 元数据、摘要、笔记和分析的混合知识库 | 不依赖 | Planned |
| v2.2 | PDF Ingestion & Full-text Pipeline | 合法 PDF 获取、解析、分块、全文索引和证据定位 | 不依赖 | Planned |
| v2.3 | Evidence-grounded Paper Analysis | 以真实证据为基础的结构化论文精读 | 不依赖 | Planned |
| v2.4 | Knowledge-base QA | 单篇、跨篇和主题问答及引用锚点 | 不依赖 | Planned |
| v2.5 | Cross-paper Research Synthesis, Baseline / Module Intelligence & Evidence-based Research Gap | 跨论文矩阵、综述和有证据的 Gap | 不依赖 | Planned |
| v2.6 | Unified AI Research Workspace | 统一已有检索、Radar、订阅、知识和分析工作区 | 不依赖 | Planned |
| v2.7 | Automated Research Delivery & Zotero Weekly Workflow | 周期发现、Zotero 归档、自动分析和交付状态 | 不依赖 | Planned |
| v2.8 | Reliability, Backup & Recovery | 备份、恢复、迁移、任务和索引可靠性 | 不依赖 | Planned |
| v3.0 | Python-native Integrated Research System | 集成 v2.0-v2.8 的完整科研生命周期 | 不依赖 | Final Target |

版本严格顺序依赖：v2.0 依赖 v1.7 Stable，v2.1 依赖 v2.0 Stable，之后每一版都依赖前一版 Stable。不得从尚未封版的前一版本直接开始下一主版本开发。

## 3. Stack v1.7 — Research Radar Stable

v1.7 的唯一目标是完成 Research Radar Stable，包括：

- historical backfill；
- annual trend；
- technical-topic evolution；
- deterministic trend analysis；
- representative papers；
- trend evidence；
- scan history；
- rescan dedup；
- topic isolation；
- zh-CN / English。

v1.7 明确不承担 Knowledge Base、PDF RAG、Zotero automation、deep paper reading、Research Gap 或 Dify removal。只有 Research Radar 语义质量人工复检和完整发布门禁明确通过后，才能创建 v1.7 Stable tag。

## 4. Stack v2.0 — Python-native Runtime Migration

### 4.1 核心验收目标

- `Dify runtime dependency = REMOVED`；
- `Python-native runtime = PRIMARY`。

迁移过程中不得先删除 Dify。必须遵守：

```text
实现 Python replacement
-> parity verification
-> regression
-> cutover
-> 最后解除 Dify runtime dependency
```

Dify 在迁移期间可以作为 legacy reference、compatibility backend 或 A/B comparison baseline，但在 v2.0 Stable 后不能继续成为 required runtime dependency。既有 `LiteratureSearchService` 已属于 Python Core，禁止借“迁移 Dify”重新实现另一套文献检索系统。

### 4.2 v2.0 的九个 Stage

#### Stage 0 — Dify Dependency Audit & Frozen Baseline

完整识别 Dify DSL、Dify HTTP endpoints、Dify environment variables、Docker dependencies、UI links、integration tests、docs references 与 runtime assumptions，并把当前行为冻结为 parity baseline。本 Stage 禁止删除 Dify。

#### Stage 1 — Python-native Runtime Boundary

建立独立的 LitWatch Python application lifecycle，覆盖 startup、shutdown、configuration、dependency injection、service lifecycle、scheduler lifecycle 与 runtime health。核心系统生命周期必须能够不依赖 Dify 工作。

#### Stage 2 — Python LLM Gateway

建立统一 `LLMGateway`，负责 provider abstraction、model selection、BYOK、timeout、有限 retry、rate-limit handling、structured output、token usage、cost metadata、safe errors 与 observability。业务代码不得直接依赖某个具体模型 SDK。

#### Stage 3 — Python Pipeline Engine

建立轻量 Python-native pipeline，仅实现 LitWatch 实际需要的 sequential steps、structured input/output、conditional step、retry、fallback、validation、persistence 与 tracing。禁止为了替代 Dify 而重造完整低代码平台。

#### Stage 4 — Structured AI Contracts & Persistence

建立统一分析协议，例如 `PaperAnalysis`，至少覆盖：

- `research_question`；
- `motivation`；
- `method`；
- `key_modules`；
- `baselines`；
- `dataset`；
- `experimental_setup`；
- `metrics`；
- `results`；
- `contributions`；
- `limitations`；
- `future_work`；
- `evidence`。

迁移期内如果 Dify backend 和 Python backend 同时存在，两者必须遵守同一业务 Schema。

#### Stage 5 — Existing Dify Workflow Migration

识别仍由 Dify 承担的真实运行行为，逐项迁移剩余的 workflow orchestration、AI analysis path、Dify-specific runtime calls 与 Dify-only contracts。继续复用现有 Python `LiteratureSearchService`，禁止复制检索核心。

#### Stage 6 — Dify / Python Parity Verification

对相同输入分别执行 legacy Dify path 与 new Python path，比较 correctness、structured output validity、failures、latency、retry behavior、persistence behavior 与 regression behavior。自然语言不要求逐字一致，但业务语义和结构化 contract 必须等价或更好。

#### Stage 7 — Default Runtime Cutover

把默认运行路径切换为 Python-native runtime。Dify 不再是默认路径；如暂时保留 compatibility flag，该标志必须默认关闭，且不得成为核心功能的运行前提。

#### Stage 8 — Dify-free Runtime Acceptance

在 Dify 完全停止时验证：

- LitWatch application starts；
- Manual Search works；
- Research Radar works；
- Subscription works；
- Weekly Digest works；
- Provider Settings works；
- Scheduler works；
- Python AI workflow works；
- all release tests pass。

最终要求为 `Dify runtime dependency: REMOVED`。历史 DSL 只作为 release artifact 保留，不得删除、重写或移动。

## 5. Stack v2.1 — Hybrid Knowledge Base

v2.1 建立 Knowledge Base Engine，覆盖 metadata、abstract、notes、structured analysis、embedding、BM25、vector retrieval、hybrid retrieval、reranking、index lifecycle 与 incremental indexing。

本版必须提供其功能所需的最低可用 API/UI，但不承担完整 PDF acquisition 或 parsing。SQLite 中的真实文献与分析状态继续是权威记录，可重建的检索索引不能成为唯一数据源。

## 6. Stack v2.2 — PDF Ingestion & Full-text Pipeline

合法来源优先支持：

- local file；
- Zotero attachment；
- OA URL；
- official publisher/open repository source。

本版覆盖 PDF identity、parsing、sections、chunking、page mapping、full-text indexing、evidence locator、parsing status、retry 与 failed-document diagnostics。

禁止绕过付费墙、盗取机构凭据或模拟非法登录。机构权限文献只能由用户在官方机构网站自行登录并合法获得，LitWatch 接收用户明确提供的本地文件或合规附件，不读取或保存机构账号、密码、验证码、Cookie 或浏览器会话。

## 7. Stack v2.3 — Evidence-grounded Paper Analysis

本版基于真实 PDF、abstract 或 metadata 建立结构化论文精读，输出：

- Research Question；
- Motivation；
- Method；
- System Architecture；
- Key Modules；
- Baseline；
- Dataset；
- Experimental Setup；
- Metrics；
- Results；
- Contributions；
- Limitations；
- Future Work；
- Evidence。

重要字段应尽量定位到 paper、section、page/chunk。事实元数据和 AI 分析必须分离；缺失字段保持 null，禁止 LLM 在无证据时补全实验事实。每次分析必须声明其 `evidence_scope`。

## 8. Stack v2.4 — Knowledge-base QA

实现 single-paper QA、cross-paper QA 与 project/topic QA。回答必须基于 Hybrid Retrieval、Evidence 和 Citation/source anchors；知识库证据不足时必须明确说明证据不足，禁止幻觉式回答或伪造引用。

## 9. Stack v2.5 — Cross-paper Research Synthesis, Baseline / Module Intelligence & Evidence-based Research Gap

本版必须建立：

- Method Matrix；
- Baseline Matrix；
- Dataset Matrix；
- Metric Matrix；
- Module Matrix；
- Limitation Matrix；
- Venue Analysis；
- Temporal Analysis。

Research Survey 应覆盖 historical development、current hot topics、representative papers、major technical routes、system/module decomposition、common baselines、datasets、evaluation metrics、important venues 与 unresolved limitations。

Research Gap 必须来自真实论文证据、结构化分析和跨论文比较：

```text
Papers
-> Evidence
-> Structured Analysis
-> Cross-paper Matrix
-> Conflicts / Missing Coverage / Limitations
-> Research Gap Candidates
-> Supporting Evidence
```

禁止让 LLM 凭空生成 Research Gap。每个 Gap Candidate 必须能够回答：为什么判断为 Gap、哪些论文支持、哪些论文报告 limitation、现有 baseline 做到了什么，以及仍缺少什么。

## 10. Stack v2.6 — Unified AI Research Workspace

LitWatch 已经存在 Web UI，因此 v2.6 不是“第一次做 Web UI”。v2.1-v2.5 每一版必须同时提供该能力所需的最低可用 API/UI。

v2.6 统一现有 Literature Search、Research Radar、Subscriptions、Weekly Digest、Knowledge Base、PDF Library、Paper Analysis、QA 与 Cross-paper Synthesis，形成统一科研工作台，同时确保业务逻辑仍由 Python Core 权威实现。

## 11. Stack v2.7 — Automated Research Delivery & Zotero Weekly Workflow

正式自动化链路：

```text
Subscription
-> Weekly Run
-> Historical Dedup
-> Recommendation
-> Zotero Adapter
-> Collection
-> Paper Analysis
-> Structured Note
-> Delivery State
```

Zotero 推荐 Collection 结构：

```text
LitWatch
└── Research Topic
    └── YYYY
        └── Wxx
```

例如：

```text
LitWatch
└── TDOA Localization
    └── 2026
        └── W33
```

工作流必须幂等，同一 `canonical_id` 不能每周重复导入。至少保存 `zotero_item_key`、`collection`、`imported_at`、`analysis_status` 与 `delivery_status`。Zotero secret 必须使用 BYOK/secure configuration，禁止写入 Git、HTML、普通日志或诊断输出。

## 12. Stack v2.8 — Reliability, Backup & Recovery

本版覆盖 backup、restore、database integrity、migration recovery、scheduler recovery、stale jobs、AI pipeline recovery、knowledge index rebuild、Zotero sync recovery、configuration backup、diagnostics 与 health check。

最终恢复演练必须能够证明：

```text
backup -> destroy test state -> restore -> data consistent
```

任何破坏性恢复测试只能在明确隔离的测试状态中执行，不能对真实用户数据进行试验。

## 13. Stack v3.0 — Python-native Integrated Research System

v3.0 不是大爆炸重写，而是 v2.0-v2.8 稳定能力的集成版本，形成完整科研生命周期：

```text
Discover
-> Monitor
-> Organize
-> Read
-> Analyze
-> Compare
-> Synthesize
-> Deliver
```

## 14. External Open-source Integration Principle

采用 **Integration-first, not Copy-first** 原则。参考已有项目时优先使用 API、Adapter、MCP、documented interface 或 independent service integration；只有确有必要时才借鉴算法或局部实现。

禁止直接整仓复制、未经许可证检查复制源码，或为追求功能数量强行合并多个项目。任何第三方代码进入 LitWatch 前必须确认 upstream repository、license、attribution、NOTICE requirement、copied/modified scope 与 compatibility。

## 15. 最终三大产品目标

### Goal 1 — Weekly Research Workflow

```text
每周检索新增论文
-> 历史去重
-> 推荐
-> Zotero 入库
-> 按周归档
-> 自动结构化分析
-> 可追踪状态
```

### Goal 2 — Research Direction Intelligence

针对一个研究方向生成并追溯：历史发展、当前热点、技术路线、Top venues、Baselines、Key modules、Datasets、Metrics、Representative papers、Limitations、Research Gap candidates 与 supporting Evidence。

### Goal 3 — External Research Ecosystem Integration

通过 Adapter/Interface 吸收 Zotero、OpenAlex、Semantic Scholar、Crossref、arXiv、PDF tools、RAG tools 与其他合法开源科研项目。LitWatch 保持自己的 Canonical Identity、Historical State、Radar、Subscription、Research Intelligence 与 Automation 核心。

## 16. 版本、Git 与发布门禁

1. 每个新版本必须从上一版本已验证并上传的 Stable tag 创建独立分支。
2. 每个 Stage 独立实现、测试和提交；禁止使用 `git add .`。
3. 每次发布前必须完成 pytest、Ruff、`git diff --check`、安全审计、真实 E2E 和人工验收。
4. Stable tag 必须是 annotated tag，并核对 local peeled commit、remote branch HEAD 与 remote peeled tag commit 一致。
5. 禁止 force push、`git tag -f`、改写历史 Stable tag 或删除旧 DSL。
6. `.env`、API Key、Token、密码、机构凭据、Cookie 与 authorization header 禁止进入 Git。
7. 原有受保护的未跟踪文件保持 untouched。
8. v1.7 未完成 Stable Finalization 前，不创建 v2.0 branch，不开始任何 v2.0 implementation。

### Tag 命名待决策项

现有 `dify-v1.0` 至 `dify-v1.7` 属于历史发布命名，不得重命名、移动或覆盖。

由于 Dify 将在 v2.0 Stable 后退出 required runtime dependency，应在 v2.0 Stable 前正式评估未来 tag 是否改用：

```text
litwatch-v2.0
litwatch-v2.1
...
```

而不是继续使用 `dify-v2.x`。本计划只记录该决策点；现在不创建任何 v2.x tag。

## 17. 当前执行顺序

1. `计划表.md` 已人工审核并入仓。
2. v1.7 Research Radar 语义质量人工复检 PASS，v1.7 已正式封版为 `dify-v1.7`。
3. v2.0 分支基于 `dify-v1.7` 创建，并按九个 Stage 完成 Python-native 运行时迁移与 Dify-free acceptance。
4. 当前 v2.0 处于 Release Candidate：自动化验收、真实 v1.7 数据迁移演练、Python-only 生命周期演练均已通过，正在等待最终人工验收。
5. 人工验收通过后，再按验收结果决定 v2.0 Stable tag 命名（默认候选为 `litwatch-v2.0`）。
6. 旧 Dify 仓库、卷、集成资产与 v1.0/v1.1 DSL 保持冻结；只有在 v2.0 人工验收通过并单独授权后，才允许执行清理。

在 v2.0 人工验收完成前，不开始 v2.1 或更高版本的实现。
